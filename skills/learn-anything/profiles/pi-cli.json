{
  "id": "pi-cli",
  "description": "Persistent browser mentor session using installed Pi RPC, typed terminating turns, atomic browser publication, and per-course model selection.",
  "selection": { "priority": 80, "requiredCommands": ["pi"], "requiredCapabilities": ["piPersistentMentor"], "preferredHarnesses": ["pi"], "preferenceBonus": 100 },
  "blocks": [
    "server.node-sse",
    "web.a2ui-canvas",
    "adapter.pi-cli"
  ],
  "mentorBridge": "pi-rpc-typed-tool",
  "preferredProtocols": ["AG-UI", "A2UI-v0.9"],
  "degraded": ["mentor-output-arrives-after-headless-turn"],
  "requiredOutcome": "usable-dynamic-adaptive-browser"
}
